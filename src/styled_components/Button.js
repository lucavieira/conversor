import styled from 'styled-components'

const Button = styled.button`
    width: 80px;
    height: 30px;

    margin-bottom: 10px;

    border: 1px solid #000;
    border-radius: 5px;

    background-color: #cecee;
    color: #1a1a1a;

    font-size: 12px;
    font-weight: 600;

    cursor: pointer;
`

export default Button